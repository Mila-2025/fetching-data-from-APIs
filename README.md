# fetching-data-from-APIs
Lesson 10 – Fetching Data from APIs  In this lesson, I built a Recipe Finder application using JavaScript and the Fetch API. The app fetches recipe data from an external API (TheMealDB) based on user input and dynamically displays the results in the UI.
